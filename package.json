{
  "name": "pigeonhole-prisma",
  "version": "1.0.0",
  "repository": "https://github.com/axelnormand/pigeonhole-prisma.git",
  "author": "axelnormand <axel@axelnormand.co.uk>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "prisma-client",
    "nexus-client"
  ],
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:introspect": "prisma introspect",
    "prisma:deploy": "prisma deploy",
    "prisma:nexus": "nexus-prisma-generate --client ./prisma-client --output ./nexus-client",
    "prisma:token": "prisma token",
    "docker:start": "docker-compose up -d",
    "docker:restart": "docker-compose restart",
    "docker:logs": "docker-compose logs"
  },
  "dependencies": {
    "prettier": "^2.0.2",
    "prisma-client-lib": "^1.34.10"
  },
  "devDependencies": {
    "nexus-prisma-generate": "^0.3.7",
    "typescript": "^3.8.3"
  }
}
