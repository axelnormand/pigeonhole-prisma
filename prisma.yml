endpoint: http://localhost:4466
datamodel: datamodel.prisma

generate:
  - generator: typescript-client
    output: ./prisma-client/

hooks:
  post-deploy:
    - prisma generate
    - npx nexus-prisma-generate --client ./prisma-client --output ./nexus-client
